#target "illustrator"#include "funcs.jsx"app.coordinateSystem = CoordinateSystem.ARTBOARDCOORDINATESYSTEM;var stateFunc=false;function doRemove(box){    var selObj = getSelectItems(box);    if(selObj.length==0) return;    for(var i=0; i < selObj.length; i++){        selObj[i].remove();    }    doRemove(box); }try{    var ab  = app.activeDocument.artboards[0];    ab.rulerOrigin = [0, 0];    var box;    if(DebugScript)         box = [279.63574218750, 95.34130859375,311.1992187500,326.67333984375];    else         box = [arguments[0],arguments[1],arguments[2],arguments[3]];    doRemove(box);}catch(e){    stateFunc=true;}if(stateFunc) "NG";else "OK";